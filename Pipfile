[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"

[dev-packages]
pre-commit = "*"

[requires]
python_version = "3.7"

[scripts]
start = "python src/server.py"
format = "pre-commit run --all-files"
